apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    volume.beta.kubernetes.io/storage-class: "nfs-client"
  labels:
    io.kompose.service: sinusbot-2
  name: sinusbot-2
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
status: {}
